both tuner.cfg and data.cfg should be in the same directory as code 
extra data and configs has all data, main data and synthetic created by me to test functionality of configs 


Take the data that needs to be tested create a data folder in the folder as code and paste corresponding data.cfg file into the same folder as code, then everyrhing works 
by default data should be in the folder named data which is in the same directory as code
However default directory for data can be changed in data.cfg files as well as all code parameters 